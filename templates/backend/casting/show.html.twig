{% extends 'backend/backend_layout.html.twig' %}

{% block title %}Casting{% endblock %}

{% block content %}
    <h1>Casting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ casting.id }}</td>
            </tr>
            <tr>
                <th>role</th>
                <td>{{ casting.role}}</td>
            </tr>
            <tr>
                <th>OrderCredit</th>
                <td>{{ casting.orderCredit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_casting_index', { 'id' : casting.movie.id }) }}"><button class="btn btn-dark my-2 my-sm-0" type="submit">Back to list</button></a>

    <a href="{{ path('backend_casting_edit', {'id': casting.id}) }}"><button class="btn btn-success" type="submit">Edit</button></a>

    {{ include('backend/casting/_delete_form.html.twig') }} 
{% endblock %}
